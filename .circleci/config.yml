version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor: win/default
    working_directory: ~/app-build
    steps:
      - checkout
      - run: dotnet build --runtime linux-x64 --configuration Release --framework netcoreapp3.1

  deploy:
    machine:
      enabled: true
    working_directory: ~/app-build
    steps:
      - run:
          name: Deploy over SSH
          command: |
            scp -r ~/app-build $SSH_USER@$SSH_HOST:$SSH_FOLDER


workflows:
  version: 2.1
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master